{
  "name": "url-parser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "URL parser and query string editor with live reconstruction.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080"
  },
  "license": "MIT"
}
